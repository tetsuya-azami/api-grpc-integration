spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useAffectedRows=true&useUnicode=true&characterEncoding=utf8&serverTimezone=SERVER&allowMultiQueries=true
    username: root
    password: root
logging:
  level:
    com:
      example:
        orderprocessing:
          repository:
            mapper:
              generated: DEBUG
grpc:
  server:
    security:
      enabled: true
      certificateChain: './tls/server1.pem'
      privateKey: './tls/server1.key'
      trustCertCollection: 'tls/client.crt'
      clientAuth: REQUIRE